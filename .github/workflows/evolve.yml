name: legacy-evolve-disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy seed evolution disabled
        run: |
          echo "Legacy unattended evolution is disabled."
          echo "All improvements must flow through branch proposals and human review."
