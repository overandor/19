# Improvement Proposals

Store one JSON file per proposal, validated against:

- `docs/schemas/improvement_proposal.schema.json`

Each proposal must include source feedback, interpreted intent, affected files, test/simulation evidence, risk notes, and rollback notes.
