-r requirements.txt
pytest>=8.1.0,<9.0
httpx>=0.27.0,<1.0
pytest-asyncio>=0.23.0,<1.0
# Capped below 0.16: that release widened ruff's *default* rule set
# (UP/I/B/BLE/S/RUF and more, previously opt-in), which turns 199
# pre-existing findings across core/, scripts/ and backend.py into CI
# failures. main is already red for this reason (run 30130065414).
# Adopting the wider rule set is a deliberate repo-wide cleanup, not a
# side effect of an unpinned linter, so it is left as its own change.
ruff>=0.4.0,<0.16
