# Runtime
fastapi>=0.110.0,<1.0
uvicorn[standard]>=0.27.0,<1.0
requests>=2.31.0,<3.0
pydantic>=2.6.0,<3.0
slowapi>=0.1.9,<1.0
python-json-logger>=2.0.7,<3.0

# memory_credit_daemon (Solana credits token, devnet/localnet by default;
# see docs/MEMORY_CREDIT_DAEMON.md) — also bundles the spl.token namespace.
# Capped below 0.40: that release drops the sync `solana.rpc.api.Client`
# (async only) and moves the SPL params dataclasses from
# `spl.token.instructions` to `spl.token.models`, so an unpinned install
# breaks collection of tests/test_memory_credit_daemon.py at import time.
# Moving past it means porting solana_submit.py to AsyncClient, which
# needs a reachable RPC endpoint to verify and so is not done here.
solana>=0.37.0,<0.40

# Dev / test (install with: pip install -r requirements-dev.txt)
